{
  "name": "safe-parse-list",
  "version": "0.1.0",
  "description": "Safely parse a string into an array, split by delimiter",
  "author": "Wes Todd <wes@wesleytodd.com>",
  "keywords": [
    "string",
    "array",
    "parse",
    "comma delimited"
  ],
  "license": "ISC",
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/wesleytodd/safe-parse-list.git"
  },
  "scripts": {
    "debug": "mocha --inspect --inspect-brk --timeout=0",
    "test": "standard && mocha",
    "prepublushOnly": "npm t",
    "postpublish": "git push origin && git push origin --tags"
  },
  "devDependencies": {
    "mocha": "^6.2.2",
    "standard": "^14.3.1"
  }
}
